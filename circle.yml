machine:
  services:
    - docker
dependencies:
  post:
    - docker build . | cat # workaround progress weirdness
test:
post:
    - python -c "import talib"
    - python -c "import theano"
    - python -c "import keras"
